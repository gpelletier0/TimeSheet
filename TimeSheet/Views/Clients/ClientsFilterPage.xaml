<?xml version="1.0" encoding="utf-8"?>

<views:ValidatorContentPage x:TypeArguments="vm:ClientsFilterViewModel"
                            xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                            xmlns:views="using:TimeSheet.Views"
                            xmlns:vm="using:TimeSheet.ViewModels.Clients"
                            xmlns:behaviors="using:TimeSheet.Behaviors"
                            x:Class="TimeSheet.Views.Clients.ClientsFilterPage" 
                            x:DataType="vm:ClientsFilterViewModel"
                            Title="Clients Filter">
    
    <views:ValidatorContentPage.Content>

        <Grid RowDefinitions="Auto, *, Auto"
              Padding="10">

            <StackLayout Grid.Row="0"
                         Spacing="10">

                <Label Text="Name" />
                <Entry MaxLength="50"
                       Text="{Binding Name}" />

                <Label Text="Contact Name" />
                <Editor MaxLength="50"
                        Text="{Binding ContactName}" />

                <Label Text="Contact Email" />
                <Entry MaxLength="254"
                       Text="{Binding ContactEmail}" />

                <Label Text="Contact Phone" />
                <Entry Keyboard="Telephone"
                       MaxLength="12"
                       Text="{Binding ContactPhone}">
                    <Entry.Behaviors>
                        <behaviors:PhoneValidationBehavior />
                    </Entry.Behaviors>
                </Entry>
                
            </StackLayout>

            <Grid Grid.Row="2"
                  VerticalOptions="Center">

                <HorizontalStackLayout HorizontalOptions="Center"
                                       Spacing="10">

                    <Button WidthRequest="80"
                            BackgroundColor="DarkSeaGreen"
                            Text="Update"
                            Command="{Binding SaveCommand}" />

                    <Button WidthRequest="80"
                            Text="Clear"
                            BackgroundColor="IndianRed"
                            Command="{Binding DeleteCommand}" />
                </HorizontalStackLayout>

                <Button WidthRequest="80"
                        HorizontalOptions="End"
                        BackgroundColor="SlateGray"
                        Text="Cancel"
                        Command="{Binding CancelCommand}" />
            </Grid>

        </Grid>
        
    </views:ValidatorContentPage.Content>
    
</views:ValidatorContentPage>