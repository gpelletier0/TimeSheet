<views:ObservableContentPage x:TypeArguments="vm:TimesheetsViewModel"
                             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                             xmlns:views="using:TimeSheet.Views"
                             xmlns:models="using:TimeSheet.Models"
                             xmlns:vm="using:TimeSheet.ViewModels.Timesheets"
                             xmlns:dtos="using:TimeSheet.Models.Dtos"
                             xmlns:converters="clr-namespace:TimeSheet.Converters"
                             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
                             x:Class="TimeSheet.Views.Timesheets.TimesheetsPage"
                             x:DataType="vm:TimesheetsViewModel"
                             Title="Timesheets">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:TimeSpanSubtractionConverter x:Key="TimeSpanSubtractionConverter" />
            <converters:TimeBillingConverter x:Key="TimeBillingConverter" />
            <toolkit:EnumToIntConverter x:Key="EnumToIntConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>

        <Grid RowDefinitions="Auto, *, Auto"
              Margin="10, 0">

            <Grid Grid.Row="0"
                  Margin="0,5,0,0"
                  ColumnDefinitions="Auto, *, Auto">

                <Picker Grid.Column="0"
                        WidthRequest="100"
                        ItemsSource="{Binding TimeFilters}"
                        SelectedIndex="{Binding SelectedTimeFilter, Converter={StaticResource EnumToIntConverter}, ConverterParameter={x:Type models:TimePeriod}}" />

                <Label Grid.Column="1"
                       Margin="10,0,0,0"
                       HorizontalTextAlignment="Start"
                       VerticalTextAlignment="Center"
                       Text="{Binding FilterNames}" />
                
                <Button Grid.Column="2"
                        ImageSource="search_icon.png"
                        Command="{Binding FilterCommand}" />
            </Grid>

            <CollectionView Grid.Row="1"
                            x:Name="EntryCollectionView"
                            Margin="0,10,0,0"
                            ItemsSource="{Binding TimesheetDtos}"
                            SelectedItem="{Binding SelectedTimesheetsDto}"
                            SelectionMode="Single">

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="dtos:TimesheetsDto">
                        <Grid RowDefinitions="Auto,Auto,Auto"
                              ColumnDefinitions="10,*">

                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Path=BindingContext.ItemTappedCommand,
                                        Source={x:Reference EntryCollectionView}}"
                                    CommandParameter="{Binding .}" />
                            </Grid.GestureRecognizers>

                            <Rectangle Grid.Row="0" Grid.Column="0"
                                       Grid.ColumnSpan="2"
                                       BackgroundColor="LightSlateGray" />

                            <Label Grid.Row="0" Grid.Column="0"
                                   Grid.ColumnSpan="2"
                                   Margin="5,0,0,0"
                                   Text="{Binding Date, StringFormat='{}{0:MMMM d yyyy}'}"
                                   FontSize="Micro" />

                            <Label Grid.Row="0" Grid.Column="0"
                                   Grid.ColumnSpan="2"
                                   Margin="0,0,5,0"
                                   HorizontalTextAlignment="End">
                                <Label.Text>
                                    <MultiBinding Converter="{StaticResource TimeSpanSubtractionConverter}"
                                                  StringFormat="{}{0:hh}h{0:mm}m">
                                        <Binding Path="EndTime" />
                                        <Binding Path="StartTime" />
                                    </MultiBinding>
                                </Label.Text>
                            </Label>

                            <BoxView Grid.Row="1" Grid.Column="0"
                                     Grid.RowSpan="2"
                                     BackgroundColor="{Binding StatusColorArgb}" />

                            <Label Grid.Row="1" Grid.Column="1"
                                   Margin="5,0,0,0"
                                   FontSize="Small">
                                <Label.Text>
                                    <MultiBinding StringFormat="{}{0:hh}:{0:mm} - {1:hh}:{1:mm}">
                                        <Binding Path="StartTime" />
                                        <Binding Path="EndTime" />
                                    </MultiBinding>
                                </Label.Text>
                            </Label>

                            <Label Grid.Row="1" Grid.Column="1"
                                   Margin="0,0,5,0"
                                   HorizontalTextAlignment="End"
                                   FontSize="Micro"
                                   Text="{Binding ProjectHourlyWage, StringFormat='{}{0:C2}/h', TargetNullValue=0.00/h}" />

                            <Label Grid.Row="2" Grid.Column="1"
                                   Margin="5,0,0,0"
                                   FontSize="Micro"
                                   Text="{Binding ProjectName}" />

                            <Label Grid.Row="2" Grid.Column="1"
                                   Margin="0,0,5,0"
                                   HorizontalTextAlignment="End"
                                   FontSize="Micro">
                                <Label.Text>
                                    <MultiBinding Converter="{StaticResource TimeBillingConverter}"
                                                  StringFormat="Total: {0:C2}">
                                        <Binding Path="EndTime" />
                                        <Binding Path="StartTime" />
                                        <Binding Path="ProjectHourlyWage" />
                                    </MultiBinding>
                                </Label.Text>
                            </Label>

                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <HorizontalStackLayout Grid.Row="2"
                                   Margin="0, 10"
                                   VerticalOptions="Center"
                                   HorizontalOptions="Center">

                <Button WidthRequest="80"
                        BackgroundColor="DarkSeaGreen"
                        Text="Add"
                        Command="{Binding AddCommand}" />

            </HorizontalStackLayout>

        </Grid>

    </ContentPage.Content>
</views:ObservableContentPage>