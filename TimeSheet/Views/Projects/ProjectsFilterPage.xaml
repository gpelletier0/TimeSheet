<?xml version="1.0" encoding="utf-8"?>

<views:ValidatorContentPage x:TypeArguments="vm:ProjectsFilterViewModel"
                            xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                            xmlns:views="using:TimeSheet.Views"
                            xmlns:vm="using:TimeSheet.ViewModels.Projects"
                            xmlns:controls="using:TimeSheet.Controls"
                            x:Class="TimeSheet.Views.Projects.ProjectsFilterPage"
                            x:DataType="vm:ProjectsFilterViewModel"
                            Title="Projects Filter" >
    
    <views:ValidatorContentPage.Content>

        <Grid RowDefinitions="Auto, *, Auto"
              Padding="10">

            <StackLayout Grid.Row="0"
                         Spacing="10">

                <Label Text="Project name" />
                <Entry Text="{Binding ProjectName}" />

                <Label Text="Hourly wage" />
                <controls:NumericEntry
                    AllowDecimal="True"
                    MaximumDecimalPlaces="2"
                    AlwaysShowDecimalPlaces="False"
                    AllowNegative="False"
                    NumericValue="{Binding HourlyWage}" />

                <Label Text="Client" />
                <Picker ItemsSource="{Binding ClientIdNameDtos}"
                        ItemDisplayBinding="{Binding Name}"
                        SelectedItem="{Binding SelectedClientIdNameDto}" />
                
            </StackLayout>

            <Grid Grid.Row="2"
                  VerticalOptions="Center">

                <HorizontalStackLayout HorizontalOptions="Center"
                                       Spacing="10">

                    <Button WidthRequest="80"
                            BackgroundColor="DarkSeaGreen"
                            Text="Update"
                            Command="{Binding SaveCommand}" />

                    <Button WidthRequest="80"
                            Text="Clear"
                            BackgroundColor="IndianRed"
                            Command="{Binding DeleteCommand}" />
                </HorizontalStackLayout>

                <Button WidthRequest="80"
                        HorizontalOptions="End"
                        BackgroundColor="SlateGray"
                        Text="Cancel"
                        Command="{Binding CancelCommand}" />
            </Grid>

        </Grid>

    </views:ValidatorContentPage.Content>
    
</views:ValidatorContentPage>